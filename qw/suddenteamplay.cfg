////////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                                //
//                                    SUDDEN TEAMPLAY LITE                                        //
//                                                                                                //
////////////////////////////////////////////////////////////////////////////////////////////////////

//set nick "jan"

//exec s_timer // Keypad-timer, this will bind all your numpad kp_0-9 numbers
exec s_teamplay // make sure you have nick set before executing this

////////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                                //
//                                     S T P _ S T Y L E                                          //
// stp_style 1 - sd_tac.cfg faithful with improvements                                            //
// stp_style 2 - 2022 New colorways (default)                                                     //
//                                                                                                //
////////////////////////////////////////////////////////////////////////////////////////////////////
stp_style 1

////////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                                //
//                                    K E Y   B I N D I N G S                                     //
// For all available binds check:                                                                 //
// https://github.com/Suddendeath-qw/suddenQuake#teamplay---suddenteamplaycfg                     //
//                                                                                                //
//                                                                                                //
////////////////////////////////////////////////////////////////////////////////////////////////////

// MUST
bind ? "stp_aol stp_report"
bind ? "stp_killme" // stp_aol can not be used with this
bind ? "stp_took"
bind ? "stp_lost"
bind ? "stp_aol stp_safe"
bind ? "stp_smart"
bind ? "stp_point"

// SHOULD
bind ? "stp_powerup"

// OPTIONAL
bind ? "stp_dropped"
bind ? "stp_aol stp_help"
bind ? "stp_aol stp_replace"
bind ? "stp_wpkilled"
bind ? "stp_youtake"
bind ? "stp_enemy_quad"
bind ? "stp_enemy_quad_ok98"
bind ? "stp_enemy_pent"
bind ? "stp_enemy_ring"
bind ? "stp_get_quad"
bind ? "stp_get_pent"
